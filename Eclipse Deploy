sudo apt -qy update
sudo apt -qy upgrade
sudo apt -qy install curl
sudo apt -qy install git
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/master/install.sh | bash
nvm install --lts
node --version
npm install -g yarn
sh -c "$(curl -sSfL https://release.solana.com/stable/install)"

# Terminali kapatıp baştan açın
# Install Rust and Cargo

curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

Enter Option 1 and wait for Installation.

solana --version
rustc --version
cargo --version

# Next, Set rpc to Eclipse Testnet 

solana config set --url https://testnet.dev2.eclipsenetwork.xyz
solana-keygen new
#enter tıkla
EVM cüzdanınıza Sepolia ETH Tokenları alın
Sepolia ETH istemek için bir Alchemy hesabı oluşturun.
Alchemy Sepolia musluğunu ziyaret edin ve Sepolia test ağı ETH'yi edinin

mkdir eclipse
cd eclipse
sudo nano deposit.js
verilerin tamamı burada---https://github.com/Eclipse-Laboratories-Inc/testnet-deposit/blob/main/deposit.js
yukarıdaki githup adresine girin tümünü koplayalın terminale yapıştır. sonrasında ctrl x y enter

 yarn install
$ npm install ethers
$ npm install bs58
$ sudo apt-get install bzip2
$ solana-keygen pubkey
(Pubkey'inizi almak için bunu çalıştırın)

node deposit.js <Pubkey> 0x7C9e161ebe55000a3220F972058Fb83273653a6e 100000000 100 <EVM Wallet Private Key> https://rpc.sepolia.org
verdiği pubkey yazan yere yapıştır, evm wallet private key yerine metamask cüzdan adresinin keyini yapıştır. >< bu işareti silin

 # Solana test doğrulayıcının çalışıp çalışmadığını kontrol edin.
solana-test-validator
ctrl-c yapın

cd ~/eclipse
git clone https://github.com/solana-labs/example-helloworld
cd example-helloworld
npm install

cd ~/eclipse/example-helloworld
sudo apt install build-essential -y
npm run build:program-rust
tamamlanlası bir kaç dakika sürebilir

# Akıllı sözleşmeyi Eclipse Testnet'e dağıtın.
solana program deploy dist/program/helloworld.so
npm run start
succses diyorsa tamamlanmış demektir.
